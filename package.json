{
  "name": "bingo-react",
  "version": "1.0.0",
  "description": "My goal is to compare and Elm program to a React one by building the same app.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Mike Gehard",
  "license": "MIT",
  "devDependencies": {
    "grunt": "^0.4.5",
    "grunt-devserver": "^0.6.0"
  }
}
